name: Smoke test
on:
  workflow_call:
    inputs:
      image_name:
        required: true
        type: string
      tag_name:
        required: false
        type: string
        default: ci_test

jobs:
  test_docker_deployment:
    runs-on: blacksmith-4vcpu-ubuntu-2404

    steps:
      - name: Build, no push
        uses: useblacksmith/build-push-action@v2
        with:
          file: Containerfile
          push: false
          tags: ghcr.io/taranis-ai/${{ inputs.image_name }}:${{ inputs.tag_name }}
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run docker compose for last commit
        env:
          IMAGE_NAME: ${{ inputs.image_name }}
          TAG: ${{ inputs.tag_name }}
        working-directory: docker/
        run: |
          docker compose -f compose.e2e.yml up --pull=never --wait-timeout=60 --wait
          echo "Echo docker ps -a:"
          docker ps -a
          echo "Echo logs:"
          docker compose -f compose.e2e.yml logs
          docker compose -f compose.e2e.yml down
